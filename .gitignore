# Rust build output
/target

# Built plugin artifacts — rebuild them from source, don't commit binaries.
/dist
*.wasm

# Editor / OS noise
*.swp
.DS_Store

# generated repository manifest (published to gh-pages by CI)
/site
